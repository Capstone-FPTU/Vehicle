from paho.mqtt import client
from object import *
from subscribe import *
import threading
import argparse
import time

# start_client = threading.Thread(target=start)
# start_client.start()
start()
time.sleep(0.5)

